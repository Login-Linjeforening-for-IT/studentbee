import Image from 'next/image'
import Link from 'next/link'

// Displays the header
export default function Navbar() {
    return (
        <div className='grid grid-rows-3 h-full w-full gap-4 bg-orange-500'>
        </div>
    )
}