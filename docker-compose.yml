version: '3.8'

services:
  nextjs-app:
    container_name: electromart
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - /app/node_modules
    environment:
      - NODE_ENV=production
    command: npm run build && npm run start