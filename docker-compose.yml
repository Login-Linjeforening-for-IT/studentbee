services:
  studentbee:
    container_name: studentbee
    build:
      context: ./frontend
      dockerfile: Dockerfile
    restart: always
    ports:
      - "8400:3000"
    env_file: .env
    depends_on:
      - studentbee-api

  studentbee-api:
    container_name: studentbee-api
    build:
      context: ./api
      dockerfile: Dockerfile
    restart: always
    ports:
      - "8401:8080"
    env_file: .env
    depends_on:
      - studentbee-postgres

  studentbee-postgres:
    image: postgres:18
    container_name: studentbee-postgres
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER:-studentbee}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB:-studentbee}
    volumes:
      - postgres_data:/var/lib/postgresql
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U studentbee -d studentbee"]
      interval: 10s
      retries: 5

volumes:
  postgres_data:
